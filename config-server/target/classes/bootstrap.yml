server:
  port: 8888

spring:
  devtools:
    livereload:
      enabled: true
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/anasabbal/mini-uber-microservice/tree/master/config-server
        default-label: master
