export default interface DriverCommand {
    firstName?: string,
    lastName?: string
};

export default interface DriverResponse {
    id?: string,
    firstName?: string,
    lastName?: string
    createAt?: string,
    updatedAt?: string
};