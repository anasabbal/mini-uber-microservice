spring:

  data:
    mongodb:
      uri: mongodb+srv://mongodb:mongodb@cluster0.lppaw8l.mongodb.net/driver-service
      auto-index-creation: true

  application:
    name: driver-service
  profiles:
    active: default
  devtools:
    restart:
      enabled: true
server:
  port: 8081